services:
  ingest:
    build:
      context: ./workers/ingest
    env_file:
      - .env.local
    # Pull-based worker: connects outbound to Supabase only.
    # No port binding needed — no HTTP server.
